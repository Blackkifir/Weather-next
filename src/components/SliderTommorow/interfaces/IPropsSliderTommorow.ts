export interface IPropsSliderTommorowAll {
  nameAddress: string,
  dateDay: string,
  iconSlider: string,
  visibility: number,
  windspeed: number,
  pressure: string,
  humidity: number,
  temp: number,

  country: string,
}

export interface IPropsWidgetsTommorow {
  dateWidgetTime: string,
  tempWidget: number,
  icon: string,
}
